@model IEnumerable<Cafeteira.Models.Cafe>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Marca)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Preco)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descafeinado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Validade)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Marca)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Preco)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descafeinado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Validade)
        </td>
                                       <!--Nome, Nome da View, Id do item-->
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CafeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CafeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CafeId })
        </td>
    </tr>
}

</table>
