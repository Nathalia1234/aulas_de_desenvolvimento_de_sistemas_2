@model IEnumerable<MoveisPlanejados.Models.Funcionario>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
         Nome do Funcionário
        </th>
        <th>
         Status
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Disponivel)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Pk_Funcionario }) |
            @Html.ActionLink("Details", "Details", new { id=item.Pk_Funcionario }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Pk_Funcionario })
        </td>
    </tr>
}

</table>
